sort_vector=function(){
  inp=scan()
  inp
  print("vector sorted in ascending order is:")
  print(sort(inp))
  print("vector sorted in descending order is:")
  print(sort(inp,decreasing=TRUE))
}
sort_vector()
